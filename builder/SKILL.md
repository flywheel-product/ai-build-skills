---
name: builder
description: A living playbook for how an AI should build software so it lands right on the first pass and cuts prompting round-trips — every rule is a failure-mode-turned-default. Generic to any app. Covers behind-the-scenes build practice and process: be self-sufficient (verify/diagnose/fix before involving the user), build & ship discipline (compile clean, serves 200, never build over a live dev server, reuse code, commit→push→live, version + release notes), and which companion skills to pull in when. Use when building or changing any app, when shipping, when verifying a change works, or when the user types /builder. Also read this skill in full when asked to suggest improvements to how the AI builds — the guide opens by explaining its own purpose and how to extend it. For visual verification/screenshots use the screenshotqa skill.
---

The full instructions for this skill are in `builder.md`, in this same folder. Read it in full and follow it — re-read it each time; do not rely on memory.

(This file is just the Claude adapter so the skill auto-triggers. The portable guide is `builder.md` and works with any AI. Licensed CC BY 4.0.)
